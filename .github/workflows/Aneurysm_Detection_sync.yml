name: Sync from Upstream

on:
  schedule:
    - cron: "10 9 * * *"   # daily at 09:10 UTC
  workflow_dispatch:

jobs:
  sync:
    uses: 2ai-lab/.github/.github/workflows/reusable-sync-upstream.yml@main
    with:
      upstream_url: https://github.com/2ai-lab/Aneurysm_Detection.git
      mode: "push"   # use "pr" if branch protection requires PRs
    secrets:
      # push_token: ${{ secrets.PAT }}   # only needed if GITHUB_TOKEN cannot push
